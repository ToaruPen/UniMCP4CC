# UIToolkit Extension (Optional)

This sample installs the `LocalMcp.UnityServer.UIToolkit.Editor` DLL into your Unity project so that `unity.uitoolkit.*` MCP tools can be used.

If you no longer need UI Toolkit MCP tooling, you can remove the imported sample folder from `Assets/Samples/`.
